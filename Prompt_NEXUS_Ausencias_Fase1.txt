 NEXUS Módulo de Ausencias — Fase 1 Prompt de implementación para Claude Code Fine Pitch de México — Proyecto NEXUS Junio 2025 · Uso interno · Confidencial Contexto general Estás trabajando en NEXUS , una app React de arquitectura single-file ( App.jsx ) con Supabase como backend. El proyecto está en C:\Users\[PC username]\Desktop\taskops . El stack es React + Vite + Supabase JS client. No uses librerías de calendario externas — todo se construye con lógica propia en React. Objetivo Implementar el Módulo de Ausencias — Fase 1 dentro de NEXUS. Esta fase cubre: Calendario de ausencias visible para todos los usuarios (semanal y mensual) Formulario de registro accesible para RH y Jefes/Gerentes de departamento 4 tipos de ausencia con soporte de recurrencia configurable Mini-widget en sidebar con ausentes del día No implementar en esta fase: flujo de solicitudes por empleado, alertas de empalme, saldo de vacaciones, notificaciones automáticas Base de datos — Supabase Ejecuta estas migraciones en el SQL Editor del proyecto mdqzyjmklawrvkghmabi . Tabla principal create table ausencias ( id uuid primary key default gen_random_uuid(), empleado_nombre text not null, empleado_iniciales text not null, departamento text not null, tipo text not null check (tipo in (&apos;vacaciones&apos;,&apos;permiso&apos;,&apos;dia_asignado&apos;,&apos;esquema_reducido&apos;)), fecha_inicio date not null, fecha_fin date not null, hora_permiso time, -- solo cuando tipo = &apos;permiso&apos; tipo_permiso text check (tipo_permiso in (&apos;entrada_tarde&apos;,&apos;salida_temprana&apos;)), recurrencia text not null default &apos;ninguna&apos; check (recurrencia in (&apos;ninguna&apos;,&apos;semanal&apos;,&apos;quincenal&apos;,&apos;mensual&apos;)), dias_semana integer[], -- [1,4] = lunes y jueves nota_interna text, -- solo visible para RH registrado_por text not null, registrado_por_rol text not null check (registrado_por_rol in (&apos;rh&apos;,&apos;gerente&apos;)), created_at timestamptz default now(), updated_at timestamptz default now() ); alter table ausencias enable row level security; create policy &quot;Lectura publica de ausencias&quot; on ausencias for select using (true); create policy &quot;Insercion por RH y gerentes&quot; on ausencias for insert with check (true); create policy &quot;Edicion por RH y gerentes&quot; on ausencias for update using (true); create policy &quot;Eliminacion por RH&quot; on ausencias for delete using (true); Función auxiliar para expandir recurrencias Ejecutar en el SQL Editor para que el calendario pueda consultar fechas efectivas dado un rango: create or replace function get_ausencias_en_rango( fecha_desde date, fecha_hasta date) returns table ( id uuid, empleado_nombre text, empleado_iniciales text, departamento text, tipo text, fecha_efectiva date, hora_permiso time, tipo_permiso text, recurrencia text, nota_interna text, registrado_por text, registrado_por_rol text ) language plpgsql as $$ declare r record; f date; intervalo interval; begin for r in select * from ausencias loop if r.recurrencia = &apos;ninguna&apos; then f := r.fecha_inicio; while f &lt;= r.fecha_fin and f &lt;= fecha_hasta loop if f &gt;= fecha_desde then id := r.id; empleado_nombre := r.empleado_nombre; empleado_iniciales := r.empleado_iniciales; departamento := r.departamento; tipo := r.tipo; fecha_efectiva := f; hora_permiso := r.hora_permiso; tipo_permiso := r.tipo_permiso; recurrencia := r.recurrencia; nota_interna := r.nota_interna; registrado_por := r.registrado_por; registrado_por_rol := r.registrado_por_rol; return next; end if; f := f + 1; end loop; elsif r.recurrencia in (&apos;semanal&apos;,&apos;quincenal&apos;,&apos;mensual&apos;) then intervalo := case r.recurrencia when &apos;semanal&apos; then &apos;1 week&apos;::interval when &apos;quincenal&apos; then &apos;2 weeks&apos;::interval when &apos;mensual&apos; then &apos;1 month&apos;::interval end; f := r.fecha_inicio; while f &lt;= fecha_hasta loop if f &gt;= fecha_desde then if r.dias_semana is null or extract(isodow from f)::integer = any(r.dias_semana) then id := r.id; empleado_nombre := r.empleado_nombre; empleado_iniciales := r.empleado_iniciales; departamento := r.departamento; tipo := r.tipo; fecha_efectiva := f; hora_permiso := r.hora_permiso; tipo_permiso := r.tipo_permiso; recurrencia := r.recurrencia; nota_interna := r.nota_interna; registrado_por := r.registrado_por; registrado_por_rol := r.registrado_por_rol; return next; end if; end if; f := f + 1; end loop; end if; end loop; end; $$; Cambios en App.jsx 1. Constante de roles con acceso al módulo Identifica en el código existente cómo está definido el array/objeto de usuarios o departamentos (busca USERS , departments , userRole o similar). Agrega un campo puedeRegistrarAusencias: true a los roles de RH y a todos los que tengan rol de gerente o jefe. Si el sistema usa una sesión por departamento: const PUEDE_REGISTRAR_AUSENCIAS = [&apos;rh&apos;, &apos;gerente&apos;, &apos;direccion&apos;] // ajusta a los valores reales del sistema 2. Nuevo ítem en el sidebar Agrega Ausencias como sección propia en el sidebar, al mismo nivel que Tareas y Avisos, con ícono de calendario. Debe aparecer para todos los usuarios (lectura), pero el botón “Nueva ausencia” dentro de la vista solo se muestra si el usuario tiene permiso de registro. &lt;button onClick={() =&gt; setCurrentView(&apos;ausencias&apos;)} className={currentView === &apos;ausencias&apos; ? &apos;active&apos; : &apos;&apos;} &gt; &lt;CalendarIcon /&gt; Ausencias &lt;/button&gt; 3. Mini-widget de ausentes hoy en el sidebar Debajo de los ítems de navegación, mostrar una sección compacta con las personas ausentes hoy. Carga desde Supabase al montar. Máximo 4 entradas visibles, con “ver más” que lleva a la vista completa. const hoy = new Date().toISOString().split(&apos;T&apos;)[0] const { data } = await supabase .rpc(&apos;get_ausencias_en_rango&apos;, { fecha_desde: hoy, fecha_hasta: hoy }) 4. Estructura del componente AusenciasView Crea el componente dentro de App.jsx (no en archivo separado, siguiendo la arquitectura existente): AusenciasView ├── Header │ (título + toggle semanal/mensual + botón Nueva ausencia si tiene permiso) ├── ResumenMetricas │ (4 cards: total ausentes, vacaciones, permisos, día asig./esq. reducido) ├── FiltrosDepartamento │ (chips filtrables — usa los mismos departamentos del sistema) ├── CalendarioSemanal (vista por defecto) ├── CalendarioMensual (toggle) └── FormularioAusencia (modal/panel lateral, condicional) Lógica del calendario Vista semanal Columnas: lunes a viernes de la semana activa Filas: una por empleado que tenga al menos una ausencia en esa semana Navegación: botones anterior / hoy / siguiente Celda de hoy con fondo diferenciado Click en pastilla → tooltip con detalle Vista mensual Grid de 5–6 semanas con todos los días del mes En cada día, mostrar chips apilados (máx 3 visibles + “N más” si excede) Navegación mes anterior / mes siguiente La pastilla muestra solo iniciales + tipo abreviado Colores por tipo de ausencia const TIPO_CONFIG = { vacaciones: { label: &apos;Vacaciones&apos;, bg: &apos;#E1F5EE&apos;, text: &apos;#0F6E56&apos; }, permiso: { label: &apos;Permiso&apos;, bg: &apos;#E6F1FB&apos;, text: &apos;#185FA5&apos; }, dia_asignado: { label: &apos;Día asignado&apos;, bg: &apos;#FAEEDA&apos;, text: &apos;#854F0B&apos; }, esquema_reducido: { label: &apos;Esquema reducido&apos;, bg: &apos;#EEEDFE&apos;, text: &apos;#3C3489&apos; }, } Cálculo de días hábiles function diasHabiles(fechaInicio, fechaFin) { let count = 0 let current = new Date(fechaInicio) const fin = new Date(fechaFin) while (current &lt;= fin) { const dow = current.getDay() if (dow !== 0 &amp;&amp; dow !== 6) count++ current.setDate(current.getDate() + 1) } return count } Formulario de registro Campos y lógica condicional Tipo seleccionado Campos que aparecen Permiso Fecha (un día) · Tipo (entrada tarde / salida temprana) · Hora · Recurrencia Vacaciones Fecha inicio · Fecha fin · Días hábiles (auto) · Recurrencia: ninguna Día asignado / Esquema reducido Fecha inicio · Fecha fin (opcional) · Días de la semana (L M X J V) · Recurrencia obligatoria Vista previa: &quot;Juan P. · Vacaciones · 16–19 Jun · 4 días hábiles&quot; — se muestra en tiempo real antes de guardar Visibilidad de nota interna Al guardar: el campo nota_interna siempre se guarda en la tabla. En el tooltip/detalle del calendario: la nota solo se muestra si el usuario actual es RH. Para todos los demás: solo nombre, tipo, fechas y quién registró. Validaciones antes de guardar Empleado seleccionado Tipo seleccionado Fecha inicio ≤ fecha fin Para permiso : hora obligatoria Para dia_asignado y esquema_reducido : al menos un día de semana seleccionado No guardar si fecha_inicio está en el pasado más de 30 días (warning, no bloqueo) Guardado en Supabase const { error } = await supabase.from(&apos;ausencias&apos;).insert({ empleado_nombre, empleado_iniciales, departamento, tipo, fecha_inicio, fecha_fin, hora_permiso: tipo === &apos;permiso&apos; ? horaPermiso : null, tipo_permiso: tipo === &apos;permiso&apos; ? tipoPermiso : null, recurrencia, dias_semana: [&apos;dia_asignado&apos;,&apos;esquema_reducido&apos;] .includes(tipo) ? diasSemana : null, nota_interna: notaInterna || null, registrado_por: currentUser.nombre, registrado_por_rol: currentUser.rol, }) Tooltip / detalle al hacer click en pastilla Mostrar un popover posicionado con la siguiente información: Visible para Contenido Todos los usuarios Nombre completo · Tipo · Fechas · Recurrencia (si aplica) · Registrado por Solo RH Nota interna · Botón Editar · Botón Eliminar Gerente que registró Botón Editar (solo su propio registro) Realtime Suscribirse a cambios en la tabla ausencias para que el calendario se actualice sin recargar cuando RH o un gerente registra algo: useEffect(() =&gt; { const channel = supabase .channel(&apos;ausencias-changes&apos;) .on(&apos;postgres_changes&apos;, { event: &apos;*&apos;, schema: &apos;public&apos;, table: &apos;ausencias&apos; }, () =&gt; { cargarAusencias() }) .subscribe() return () =&gt; supabase.removeChannel(channel) }, []) Notas de implementación Regla Detalle Sin librerías externas No usar react-calendar, fullcalendar, etc. La lógica de fechas va en JS nativo. Sin archivos nuevos Todo dentro de App.jsx siguiendo la arquitectura existente. Roles y usuarios Leer el código existente antes de escribir. Adaptar a la estructura real del sistema. Selector de empleados Usar la misma fuente de datos que ya existe (array local o tabla Supabase). Estilos Si el proyecto usa Tailwind, usarlo. Si usa clases propias, seguir el patrón existente. Verificación final Correr npm run build al terminar y confirmar que no hay errores. Orden de ejecución recomendado Ejecutar las migraciones SQL en Supabase (tabla + función) antes de tocar App.jsx Pegar el prompt en Claude Code Claude Code debe leer App.jsx antes de escribir código para identificar roles, usuarios y estructura existente Verificar que la suscripción Realtime se limpia correctamente en el useEffect Correr npm run build y confirmar sin errores antes de hacer vercel --prod NEXUS — Fine Pitch de México · Documento interno · Junio 2025 
